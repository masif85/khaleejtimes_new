<figure class="figure" data-uuid="{{ uuid }}"
        data-width="{{ width }}"
        data-height="{{ height }}">
    <img class="figure-img img-fluid"
         srcset="{{ srcset }}"
         src="{{ src }}"
         alt="{{ alttext }}"/>
</figure>
